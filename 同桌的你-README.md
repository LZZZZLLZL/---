# 同桌的你 · 视觉小说（单文件版）

## 文件

- `同桌的你（视觉小说）.html` —— 最终成品，双击即玩，离线可用，可直接发给任何人
- `vn_src/` —— 源码目录（改剧情/美术后需重新构建）
- `vn_assets/` —— AI 美术资产：提示词、生成与后处理流水线

## 源码结构

| 文件                 | 作用                                                    |
| -------------------- | ------------------------------------------------------- |
| `vn_src/icons.js`    | Tabler Icons (MIT) 内嵌 SVG 图标库，`VNICON.icon(name)` |
| `vn_src/story.js`    | 剧本：节点图、选项、好感度(aff)、结局注册               |
| `vn_src/art.js`      | 美术库：内嵌位图资产优先，SVG 矢量插画兜底              |
| `vn_src/audio.js`    | Web Audio 程序化 BGM 与音效（BGM/音效独立开关）         |
| `vn_src/main.js`     | 引擎：打字机、选项、好感度路由、多存档位、图鉴、设置    |
| `vn_src/engine.html` | 页面外壳 + 样式（`<!--VN_SCRIPTS-->` 为注入点）         |
| `vn_src/build.js`    | 构建脚本：内联 JS + 把 `vn_assets/img` 图片 base64 内嵌 |

## 游戏功能

- **多存档位**：1 个自动存档（剧情推进时自动覆盖）+ 6 个手动存档位；存读档面板分页显示（每页 3 个手动位），含剧情文本预览、保存时间、单条删除；旧版单存档自动迁移到「存档位 一」
- **设置面板（双栏）**：
  - 声音栏：背景音乐开关、音效开关、音量
  - 显示栏：文字速度、打字效果、男主立绘（默认关）、自动保存
  - 底部操作：返回标题、退出游戏（退出前自动存档）
- **演出细节**：背景跟随（节点未声明 bg 时沿用当前背景，不回退默认场景）、说话人名牌、好感度提示 toast、结局图鉴（隐藏结局机制）
- **图标**：全部使用内嵌 SVG 线性图标（Tabler Icons），无 emoji，单文件离线可用
- **键盘操作**：空格/点击推进 · 数字键 1-3 快速选择 · Esc 关闭弹窗

## AI 美术流水线（vn_assets/）

| 文件             | 作用                                                                                  |
| ---------------- | ------------------------------------------------------------------------------------- |
| `make_assets.py` | 批量生成 + 后处理（背景 16:9 智能裁切、立绘色度键抠图 + despill + 白描边、WebP 压缩） |
| `img/bg/`        | 背景 & CG（1280×720 WebP）                                                            |
| `img/char/`      | 立绘（1100×1300 RGBA WebP，透明背景 + 白色描边，同角色固定画布基线）                  |
| `raw/`           | 生成原图（1536×1024 / 1024×1536 PNG，重处理时免二次调用 API）                         |
| `fetch_icons.py` | 从 Tabler Icons 下载 SVG 并生成 `vn_src/icons.js`                                     |

```bash
cd vn_assets
python make_assets.py --list            # 查看资产生成状态
python make_assets.py                   # 生成所有缺失资产（可中断续跑）
python make_assets.py --force bg_class  # 强制重新生成指定资产
python make_assets.py --only ch_linwei_smile --reprocess  # 用 raw 原图重新后处理（不调 API）
```

依赖：`.env` 中的 `JMRAI_API_KEY`，`openai`、`Pillow`（WebP）。

处理要点：

- 背景/CG：API 请求原生 1536×1024 横版，居中裁切为 16:9，缩放 1280 宽压缩。
- 立绘：模型不支持透明背景，改用纯色底生成 → BFS 色度键 + 封闭死角全局绿键 + 邻域 despill 去绿边 → alpha 膨胀垫白色剪影形成白描边 → 固定 1100×1300 画布底部居中（保证表情切换基线一致）。
- 构建时全部图片转 base64 data-URI 内嵌进单文件 HTML，保持"双击即玩、离线可用"。

```bash
cd vn_src
node build.js     # 重新构建单文件 HTML
node verify.js    # 校验剧本图与脚本语法
```

## 结局一览（含剧透）

| 结局             | 条件                                 |
| ---------------- | ------------------------------------ |
| 同桌的你         | 默认（诺言线好感 <3）                |
| 说出口的夏天     | 银杏树下选"说出喜欢"                 |
| 一步之约         | 银杏树下选"先读明天"                 |
| 晨光             | 诺言线且好感 ≥3                      |
| 写诗的人（隐藏） | 诺言线且好感满 4（4 次心动选择全对） |

好感度来源：留糖 +1 / 写下愿望 +1 / 回复"同桌嘛" +1 / 回信"一直做同桌" +1 / 请她讲题 +1
